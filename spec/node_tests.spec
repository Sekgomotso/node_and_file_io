var {Visitor} = require('..src/main.js');
var motso = new Visitor;

describe("function that saves visitor's data", () => {
    it("should return visitor's full name", ()=>{
        expect("motso.save()").toBe(visitor_motso.json)
    })
})